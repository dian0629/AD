- name: REMOVE AD SERVICES FEATURE
  hosts: DC1
  gather_facts: yes

  tasks:
    - name: Install AD Services feature
      win_feature:
        name: AD-Domain-Services
        include_management_tools: yes
        include_sub_features: yes
        state: absent
      register: result
      
    - name: Reboot
      win_reboot:
      when: result.reboot_required
